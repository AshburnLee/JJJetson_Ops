# cuda-ops 的 pip 包快照（Jetson Orin，2026-08-27）
# 虚拟环境本身不在这个文件里，在 environment.yml（python / pybind11 / ruff / pre-commit）。
# 本文件只列 pip 装的包；conda 已经装过的不要再写进来，避免两套抢同一个包。
# 注意：torch==2.6.0+cu126 是本机 CUDA wheel，换机器/换架构不能直接 pip 装这一行。
annotated-doc==0.0.5
anyio==4.14.2
certifi==2026.7.22
click==8.5.0
filelock==3.29.0
fsspec==2024.6.1
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.29.0
idna==3.19
Jinja2==3.1.4
markdown-it-py==4.2.0
MarkupSafe==2.1.5
mdurl==0.1.2
mpmath==1.3.0
networkx==3.3
numpy==2.1.2
packaging==26.3
pillow==11.0.0
Pygments==2.21.0
regex==2026.7.19
rich==15.0.0
safetensors==0.8.0
shellingham==1.5.4
sympy==1.13.1
tokenizers==0.23.1
torch==2.6.0+cu126
torchvision==0.21.0
tqdm==4.70.0
transformers==5.16.1
typer==0.27.1
